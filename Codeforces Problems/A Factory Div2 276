#include<iostream>
#include<cmath>
#include<string>

using namespace std;

int main()
{
	long long a, b;
	cin >> a >> b;
	for (int i = 0; i < 40; i++)
		a *= 2;
	if (a% b == 0)
		cout << "Yes";
	else
		cout << "No";

	return 0;
}
